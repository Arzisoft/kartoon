# Session renders — 2026-08-17

Intermediate/debug renders from the local polish session (retopology verification, read tests,
beak/blanket iteration debugging, a broken first toon-shader attempt). Archived so `build/`
only carries the current result. `lineup_polish.png` (in `build/`, not here) is the current
best state of both characters.
